# PRD｜习惯养成规划板

> **项目**：`my-website`（GitHub：ChouKaguya-hime/my-website）
> **版本**：v1.0 ｜ **日期**：2026-09-18（Day 4）｜ **作者**：斌（与 AI 协作）
> **上游文档**：`research.md`（Day 3 需求研究）
> **本文只回答三个问题**：做什么、不做什么、怎么算做完了。
> 本文**不写代码、不定技术方案**（技术选型是 Day 5 的事）。

---

## 0. 读之前先约定三件事

1. **「本期」= 第 1 周**，上限是 Day 7 能在浏览器里打开的 MVP。本文所有「不做」都是指本期不做，不等于永远不做。
2. **验收标准统一编号 AC-x**，写法固定为「做什么动作 → 看到什么结果」，不允许出现「体验流畅」这种没法验的话。
3. **与 `research.md` 冲突时以本文为准**，但本文不得违反 `research.md` 定下的三条设计原则（见第 7 节）。

---

## 1. 一句话定位

> 一个**只给自己用**的习惯养成规划板：把「我想坚持做的事」和「我必须处理的事」放在同一屏，
> 用**不会因为漏一天就崩掉**的进度反馈，让我愿意一直用下去。

**重点不在记得全，在于漏了之后我还愿意继续打开它。**

### 1.1 要解决的问题（沿用 research.md，按痛感排序）

| 编号 | 问题 | 具体表现 | 怎么算解决了 |
| --- | --- | --- | --- |
| P1 | 坚持不下去 | 漏一天就觉得前功尽弃，然后彻底不管了 | 漏一天之后我还会打开它 |
| P2 | 事情多、理不清 | 待办散在便签、聊天记录和脑子里，一想到就焦虑 | 打开一屏就能看清今天做什么、还剩什么 |
| P3 | 工具把习惯和待办割开 | 一边打卡 App、一边待办 App，维护两套越用越累 | 在同一处既勾习惯、又勾待办 |

### 1.2 明确不解决的问题（划出去，别往这上面使劲）

- ❌ 不解决「多个人一起用」——本期只有一个用户：我
- ❌ 不解决「提醒我」——不做推送、不做通知
- ❌ 不解决「给我动力」——不做游戏化、不做社交压力
- ❌ 不解决「让我更专注」——不做番茄钟，那是另一个产品

---

## 2. 用户与使用场景

**用户只有一个：我自己。** 不研究多用户、不研究陌生人上手成本。

| 项 | 结论 |
| --- | --- |
| 设备 | 桌面笔记本 + Chrome / Edge |
| 端 | 桌面优先。窗口缩窄不崩就行，**不做移动端专门适配** |
| 网络 | 打开页面即可，功能不依赖网络 |
| 账号 | 没有账号，没有登录 |

### 一天里的三个时刻（这决定界面的优先级）

| 时刻 | 我在干什么 | 时长 | 对界面的要求 |
| --- | --- | --- | --- |
| 早上 | 打开看今天要做什么 | 30 秒 | 一屏看完，不用点、不用滚 |
| 白天 | 做完一件事随手勾掉 | 5 秒 | 勾选按钮要在最顺手的位置，勾完立刻有反馈 |
| 晚上 | 想起来补勾一下 | 10 秒 | 允许补勾过去的日期，不弹「你迟到了」 |

**不在的场景**：多人协作、跨设备同步、手机上用、给别人看。

---

## 3. 目标与非目标

### 3.1 本期目标（可衡量）

| 编号 | 目标 | 怎么算达成 |
| --- | --- | --- |
| G1 | 打开就能看到今天要做的全部事情 | 首屏（不滚动）同时看到习惯区和待办区 |
| G2 | 漏几天之后我仍然愿意继续勾 | 构造「最近 3 天都没完成」的数据，界面不出惩罚性反馈，强度不清零 |
| G3 | 新增一个习惯不用想 | 只需要 1 个必填项（名称），频率有默认值 |
| G4 | 数据关掉浏览器还在 | 关掉再打开，之前加的习惯和待办原样都在 |

### 3.2 ⭐ 非目标：我在 PRD 里砍掉了什么，为什么砍

**这是本文最重要的一节。** 判断标准只有一条：

> **它能不能证明「漏一天之后我还愿意继续用」这件事？不能证明的，砍。**

#### A. 产品级砍掉的功能

| # | 砍掉的功能 | 我原本想做什么 | 砍它的理由（一句话） | 本期用什么替代 |
| --- | --- | --- | --- | --- |
| 1 | **打卡日历 +「连续 X 天」大数字** | 中间放一个「连续打卡 N 天」的大数字，断一天就归零 | **归零就是惩罚性反馈**。破链式设计会把人从「想变好」逼成「怕断」，真断了就产生「反正已经断了」的心理然后彻底放弃——这正是我要解决的 P1 问题本身 | 改成 **「本周强度 %」+ 最近 7 天格子**（漏了往下走一点，不归零） |
| 2 | **游戏化**（等级、积分、勋章、宠物、装备） | 加点积分等级勋章，应该能提升留存 | 游戏化是**用复杂度换动力**。我不玩游戏，这份代价换不来我的动力，只会让界面更重 | 只保留「今天完成了几件」这种一眼能懂的进度感 |
| 3 | **社交**（好友、排行榜、组队打卡） | 互相监督，有人盯着就不敢断 | 这是 Habitica 的留存引擎，也是它复杂的根源。**一个用户的产品不需要社交契约** | 不要，接受「没人监督」 |
| 4 | **账号注册 / 登录 / 云端同步** | 登录后换台电脑也能用 | 只给自己用，本地打开就要能用。为一个用户搭一整套后端不划算 | 数据存浏览器本地（见第 6 节） |
| 5 | **手机 App / 小程序** | 随时随地打卡 | 先把网页版跑通，多端是第 2 周之后的事 | 不做 |
| 6 | **番茄钟 / 专注计时** | 顺手加个计时器 | 和「养成习惯 + 理清待办」无关，那是另一个产品 | 不做 |
| 7 | **日历视图 / 四象限 / 甘特图** | 待办按重要紧急排四格 | 待办量大到几十条才需要。我现在的量「今天 + 本周」就够 | 等待办超过 50 条再说 |
| 8 | **数据统计报表 / 数据导出** | 像 Loop 那样导出 CSV | 本期用不上。Loop 能做是它的加分项，不是我的必选项 | 不做 |
| 9 | **消息推送 / 手机提醒** | 到点提醒我打卡 | 要系统权限或后端，本期成本高于收益 | 不做 |
| 10 | **深色模式 / 主题换肤 / 多语言** | 好看一点 | 属于打磨，不影响「有没有解决问题」 | MVP 验收通过之后再说 |
| 11 | **AI 建议 / 智能排期** | 让 AI 帮我排今天做什么 | 想法很好，但会明显拖慢 Day 7 出 MVP | 长期再说 |

#### B. 功能级砍法（这个更关键：不是砍掉整个功能，是把功能砍小）

| # | 原本会写成什么 | 砍成什么 | 为什么 |
| --- | --- | --- | --- |
| 12 | 习惯支持复杂重复规则（隔周、每月第几个周几、自定义周期） | 只留两档：**每天 / 每周 N 次** | 规则越想越全，用户（我）在「建习惯」这一步就开始配置，直接违反设计原则 3「默认零配置」；而且这会让 Day 7 出不来东西 |
| 13 | 习惯详情页放完成率图表、月度趋势、最佳连续记录 | 只放**最近 28 天格子 + 本周强度 + 编辑/删除** | 图表属于「报表」，已被第 8 条砍掉；详情页只需要回答「我最近怎么样」 |
| 14 | 待办支持优先级、标签、截止时间、子任务、备注 | 只留**一句话文本**（加创建日期和完成状态） | 加一个字段就多一倍界面和逻辑。本期要验证的是「一个入口看全」能不能降低焦虑，不是待办管理能力 |
| 15 | 习惯可以拖动排序、分组 | 只按创建时间排列 | 排序是「好用」层面的需求，不是「有没有解决问题」层面的 |

#### C. 我用的三条砍功能标准（下次也能照用）

1. **不为「完整性」留功能，只为「能不能验证核心痛点」留功能。** 一个功能如果验证不了 P1/P2/P3 任意一条，本期就不做。
2. **需要后端才能做的，本期一律砍。** 因为本期没有后端。
3. **违反三条设计原则的，一律砍。** 原则见第 7 节。

> 被砍掉的东西都记在 `research.md` 第六节里，**写了「什么时候再考虑」**，不是删掉就忘。
> 以后我想加功能，先过这一节——本文就是判决书。

### 3.3 如果我今天／这几天做不完（提前定好的降级方案）

**P0 底线**：只做「今日页 + 习惯详情 + 一项写入功能（快速加待办）」的完整闭环，其余全部列成后续清单，不硬塞进 Day 7。

---

## 4. 功能范围（本期做什么）

四个功能，标了优先级。**P0 是底线，P1 有余力再做。**

### F1 今日页（首页）— 一个入口看全 ｜ 优先级 P0

> **用户故事**：作为一个「早上刚打开电脑的人」，我想在一屏之内看到今天要做的习惯和待办，
> 这样我不用在几个页面之间来回切，也知道今天还剩什么，不会因为「不知道还有多少事」而焦虑。

**做什么**

- 顶部一行概览：今天的日期 + 「今天已完成 3 / 7 件」
- 上半区：**今天的习惯**，每行 = 习惯名 + 本周强度小字（%）+ 最近 7 天的小格子 + 一个完成按钮
  （强度就摆在今日页上，所以勾选后能立刻看到它变化——这是 AC-4 要求的东西）
- 下半区：**今天的待办**，每行 = 一句话 + 一个勾选框
- 底部一个常驻输入框：输入一句话 → 回车 → 立刻作为待办出现在列表里（这是本期的「写入功能」）
- 空数据时：显示一句「还没有习惯，先加一个试试」+ 一个示例习惯的快捷入口（**不弹窗、不强制**）

**不做什么**

- 不做拖动排序、不做分组、不做标签、不做批量操作
- 不做「今日以外」的视图（本周列表也不做，本期只有今天）

**验收标准**：AC-1、AC-2、AC-3、AC-4、AC-7（AC-13 是全局项，见第 8 节）

---

### F2 习惯详情 — 看得见进度，但不清零 ｜ 优先级 P0

> **用户故事**：作为一个「昨天忘了打卡的人」，我想看到这周真实的完成情况、而不是一个刺眼的零，
> 这样我不会觉得前功尽弃，明天还愿意打开它。

**做什么**

- **本周强度**：一个百分比（公式见第 6.2 节）+ 一条进度条
- **最近 28 天格子**：完成 = 实心；已过去但没完成 = 浅灰；未来 = 空白
- **补勾**：已过去的日期格子可以点，点一下在「完成 / 未完成」之间切换；**未来的日期点不动**
  （对应第 2 节「晚上想起来补勾一下」这个场景——不弹「你迟到了」，不记惩罚）
- 编辑：改名称、改频率（每天 / 每周 N 次）
- 删除：**必须二次确认**

**不做什么**

- ❌ 不出现「连续天数」「中断」「归零」「失败」任何一处
- ❌ 未完成不用红色、不用叉号、不用感叹号（用中性灰）
- ❌ 不做完成率图表、不做月度趋势、不做最佳记录

**验收标准**：AC-5、AC-6、AC-11、AC-12

---

### F3 快速添加待办 — 一句话就完事 ｜ 优先级 P0

> **用户故事**：作为一个「脑子里突然冒出一件事的人」，我想用一句话把它记下来，
> 这样我不用先想「该放哪个分类」，念头也不会跑掉。

**做什么**

- 输入框输入文本 → 回车（或点「加」）→ 立刻出现在待办列表最上方，输入框清空
- 文本为空时不允许提交
- 勾选完成后该条**保留在原位变灰**（不移到别处——移走会让人找不到刚才勾掉的是哪条，也省掉一套排序逻辑）

**不做什么**：不做优先级、不做截止时间、不做子任务、不做备注、不做分类

**验收标准**：AC-7

---

### F4 新建 / 编辑习惯 — 两步建好 ｜ 优先级 P1

> **用户故事**：作为一个「想开始一个新习惯的人」，我想只用最少的输入就建好一个习惯，
> 这样我不会在配置界面上卡住，也不会因为要填太多而放弃。

**做什么**

- 必填：名称。选填：频率（**默认「每天」**，可改成「每周 N 次」）
- 保存后立即出现在今日页
- 编辑：复用同一个表单，字段同上

**不做什么**：不做提醒时间、不做分类/标签、不做颜色主题、不做目标值（次数/时长/数量）、不做引导流程

**验收标准**：AC-8、AC-9、AC-10

---

## 5. 页面与导航（两个页面 + 一个弹层）

| 页面 | 内容 | 怎么到 |
| --- | --- | --- |
| **今日页** | 今日概览 + 习惯区 + 待办区 + 添加框 | 打开网页即是 |
| **习惯详情** | 强度 + 28 天格子 + 编辑/删除 | 从今日页点习惯名进入，可返回 |
| **新建 / 编辑习惯表单** | 名称 + 频率（默认每天） | 从今日页的一个按钮打开，**是弹层不是独立页面** |

**导航原则**：返回按钮永远在左上角同一个位置；不需要浏览器后退也能走完全部流程。

---

## 6. 数据与状态

| 项 | 本期结论 | 为什么 |
| --- | --- | --- |
| 存哪 | 浏览器本地存储（localStorage） | 本期不做后端、不做账号（已砍）；数据库是 Day 23 的任务 |
| 联网 | 不需要。页面加载后不再请求外部资源 | 「只给自己用」，数据不出本机 |
| 多端 | 不做。换台电脑数据不在，接受 | 已砍 |

### 6.1 存什么（两个列表，都是纯文本为主）

**习惯 habit**

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `name` | 名称 |
| `freqType` | `daily`（每天）/ `weekly`（每周 N 次） |
| `freqCount` | 每周次数，仅 `weekly` 时有效 |
| `createdAt` | 创建日期 |
| `doneDates` | 已完成日期的列表，格式 `YYYY-MM-DD` |

**待办 todo**

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `text` | 一句话 |
| `date` | 归属日期 `YYYY-MM-DD` |
| `done` | 是否完成 |

### 6.2 习惯强度怎么算（**我自己定义的简化公式**）

> ⚠️ 这不是抄 Loop Habit Tracker 的算法（我只看了它的功能描述，没读源码），
> 这是我自己定的一个够用就行的规则。行为依据见 `research.md` 第 4 节（Lally 2010：习惯自动化中位数 66 天）。

**先统一叫法（避免 Day 7 实现时理解不一致）**：本文里「本周强度」= 「最近 7 天完成率」，
是一个**滚动的 7 天窗口**，**不是自然周**（周一到周日）。全文只用「本周强度」这一个词。

```text
本周强度 = 最近 7 天内「完成的天数」÷「应该完成的天数」× 100%
           （四舍五入到整数，上限 100%）

· 每天型习惯（daily）：分母 = 7
· 每周 N 次型习惯（weekly）：分母 = N，算出来超过 100% 也按 100% 显示
· 分母为 0 时（理论上不会出现）：显示「—」
· 最近 7 天一次都没完成时：显示「—」，不显示 0%
  「—」的含义是「还没开始」，不是「失败」——这是第 7 节原则 1 的直接落点
```

**关键约束：任何一个数字都不能因为漏打卡而变成 0。**

### 6.3「今天该做哪些习惯」怎么判断（不留空，直接定死）

| 习惯类型 | 今天出现在今日页吗 |
| --- | --- |
| `daily`（每天） | 每天出现 |
| `weekly`（每周 N 次） | **也每天出现**，不指定具体星期几；最近 7 天做够 N 次就算达标 |

这么定有两个原因：一是**不用让用户在创建习惯时挑星期**（原则 3「默认零配置」）；
二是我打开页面永远能看到全部习惯，不会因为「今天没安排」就把它忘了。

---

## 7. 界面与交互原则（三条设计原则落到硬约束）

来源：`research.md` 第五节。这三条不是口号，每条都对应能在界面上验证的约束。

| 设计原则 | 落到界面上的硬约束 | 怎么验 |
| --- | --- | --- |
| 1. 漏一天不能有惩罚性反馈 | 页面文案**不得出现**「连续」「中断」「归零」「失败」；未完成一律用中性灰，不用红色/叉号 | 在页面源码里搜索这四个词，结果必须为 0 |
| 2. 一个入口看全 | 今日页**首屏不滚动**就能同时看到习惯区和待办区 | 1280×720 窗口打开，不滚动截图 |
| 3. 默认零配置 | 首次打开**不弹任何配置窗**；新增习惯最多 2 个输入框，且只有名称必填 | 清空数据后打开页面观察 |

**另外三条文案约定**（属于「说人话」）：用「你」称呼用户、不用专业术语、给数字不给形容词。

---

## 8. ⭐ 验收标准（AC）

> 每条都写成「做什么动作 → 看到什么结果」，能在浏览器里当场验一遍。
> **不接受**「体验流畅」「界面美观」这类没法验的写法。

| 编号 | 验的是哪条原则/目标 | 怎么做（动作） | 通过标准（结果） | 优先级 |
| --- | --- | --- | --- | --- |
| **AC-1** | 零配置 | 直接双击打开 `index.html` | 没有注册/登录/引导界面，3 秒内出现今日页 | P0 |
| **AC-2** | 零配置 | 清空本地数据后打开 | 不出现任何强制填写的弹窗；空状态给出「先加一个习惯」和示例入口 | P0 |
| **AC-3** | 一个入口看全（G1） | 1280×720 窗口打开今日页，**不滚动** | 习惯区和待办区同时在首屏内可见 | P0 |
| **AC-4** | 即时反馈 | 点某个习惯的完成按钮 | 该行立刻变成已完成样式，顶部计数和本周强度立刻变化，**不需要刷新** | P0 |
| **AC-5** | **漏一天不清零（G2，核心）** | 构造数据：某个习惯最近 3 天都没完成，然后打开页面 | 强度数值下降但**不等于 0**（或显示「—」）；页面上**没有**红色警告、没有失败提示、没有「归零」类文案 | P0 |
| **AC-6** | 文案黑名单 | 在页面源码里搜索「连续」「中断」「归零」「失败」 | 四个词命中数均为 **0** | P0 |
| **AC-7** | 快速添加（P2） | 在输入框输入一句话 → 按回车 | 该条立刻出现在待办列表顶部，输入框被清空；输入为空时按回车**不新增** | P0 |
| **AC-8** | 默认零配置（G3） | 点「新建习惯」，只填名称就保存 | 保存成功并出现在今日页；频率字段有默认值，不改也能存 | P0 |
| **AC-9** | 数据本地留存（G4） | 加 1 个习惯 + 1 条待办，关闭浏览器再打开 | 之前的数据原样还在 | P0 |
| **AC-10** | 数据不出本机 | 打开浏览器开发者工具的 Network 面板，刷新页面 | 除页面自身文件外，**没有任何外部网络请求** | P0 |
| **AC-11** | 补勾过去的日期 | 在习惯详情里，点 28 天格子中一个已过去、还没完成的日期 | 该日期立刻变成已完成；再点一下变回未完成；**点未来的日期没有任何变化** | P0 |
| **AC-12** | 删除要确认 | 删除一个习惯 | 先出现二次确认；确认后消失，取消则保持不变 | P1 |
| **AC-13** | 不崩 | 把窗口宽度拖到约 360px | 内容能正常滚动和点击，按钮不重叠、不消失（**不要求好看**） | P1 |

### 8.1 本期算不算成功

- **必须通过**：AC-1 ~ AC-11（全部 P0）通过 → 本期目标达成
- **加分项**：AC-12、AC-13 通过
- **一句话标准**：Day 7 时我能用浏览器打开它，**用满 3 天还没删掉**

---

## 9. 风险与假设（提前认下来，别到时候当成意外）

| 风险 / 假设 | 如果错了会怎样 | 本期怎么应对 |
| --- | --- | --- |
| 浏览器本地存储容量有限、清缓存会丢数据 | 数据可能丢 | 本期数据量极小（几十条），够用；Day 23 换数据库 |
| 「习惯强度」公式是我自己定的，可能不好用 | 反馈失真，反而没动力 | 先用这个简单公式，自己用几天再调；已记入待核实 |
| 「漏一天不清零」到底能不能留住我，**未经验证** | 核心假设不成立，产品白做 | Day 7 之后自己用 3 天再判断，这是本产品唯一能做的验证 |
| 只有一个用户，没人帮我挑错 | 容易自嗨 | 接受。本期目标本来就是「给自己用」 |
| 我会忍不住想加功能 | 拖慢 MVP，Day 7 出不来东西 | 第 3.2 节就是判决书；想加功能先过那三条标准 |

---

## 10. 待核实 / 暂不当结论用

1. **强度公式**：没有一手依据，是我自己简化定的（Loop 的算法我没读源码）。
2. **P1 假设**（我属于「漏一天就放弃」的类型）：这是自我推测，尚未确认。
3. **TickTick / Habitica 的价格**：`research.md` 里三个来源不一致——不影响本期，因为这两个产品的东西本期都不做。

---

## 11. 之后再说（明确不是本期，按「什么时候看」排）

| 想做的事 | 什么时候再看 |
| --- | --- |
| 接数据库、云端同步 | Day 23 |
| 手机端 / 多端 | 第 2 周之后 |
| 数据导出（CSV） | MVP 验收通过之后 |
| 深色模式 / 主题 | MVP 验收通过之后 |
| 日历视图 / 四象限 | 待办超过 50 条时 |
| 社交、游戏化、AI 建议 | 不确定，可能永不做 |

---

## 12. 变更记录

| 版本 | 日期 | 改了什么 |
| --- | --- | --- |
| v1.0 | 2026-09-18 | 首版（Day 4 产出）：定下 F1–F4 功能范围、15 项砍功能清单、13 条验收标准（P0 共 11 条） |
| v1.0.1 | 2026-09-18 | AI 自检后修正 7 处：补上「补勾过去日期」这个漏掉的功能和对应的 AC-11；定死「本周强度 = 最近 7 天滚动窗口」和「每周 N 次的习惯也每天都出现」两条规则；定死已完成待办保留原位变灰；修正砍功能条数统计与章节层级 |

---

**下一步**：Day 5 技术选型（用什么技术实现本文的功能，**不在今天做**）；Day 7 按本 PRD 做出能打开的 MVP。
