# my-website

我的第一个 GitHub 项目。目前是 Day 2 的占位状态，功能从 Day 7 开始搭建。

## 当前状态

| 文件 | 作用 |
| --- | --- |
| `index.html` | 占位页，Day 7 会改造成 MVP 页面 |
| `research.md` | 需求研究（Day 3）：3 个类似产品比较 + 本期不做清单 |
| `.gitignore` | 忽略规则，确保 `.env` 等本地文件不会被上传 |
| `.env` | 本地环境变量（**已被忽略，不在仓库里**），Day 23 接数据库时启用 |

## 本地预览

直接用浏览器打开 `index.html` 即可，暂时不需要任何服务器或依赖。

## 提交规范

提交标题以「Day N｜」开头，例如：

```
Day 2｜初始化仓库并添加 index.html 占位页
```

正文两行：改了什么 / 加了什么。
