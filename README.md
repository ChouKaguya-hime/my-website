# my-website

我的第一个 GitHub 项目。目前是 **Day 7** 的状态：**MVP 已经能跑起来了**——今日页、习惯详情、待办三块都通了，数据存在浏览器本地。

## 当前状态

| 文件 | 作用 |
| --- | --- |
| `index.html` | **今日页（MVP 入口）**：顶部概览 + 习惯区 + 待办区 + 底部输入框，另含习惯详情、新建/编辑、删除确认三个弹层 |
| `style.css` | 全部样式（只用系统字体，不引任何外部资源） |
| `app.js` | 全部逻辑：读写浏览器本地存储、算「本周强度」、重画页面 |
| `AGENTS.md` | 项目规则（给 AI 的工作手册）：Day 1 的沟通/工作规则 + Day 6 追加的边界规则 |
| `research.md` | 需求研究（Day 3）：3 个类似产品比较 + 本期不做清单 |
| `PRD.md` | 产品需求文档（Day 4）：做什么 / 不做什么 / 怎么算做完了（含 13 条验收标准） |
| `TECH_DESIGN.md` | 技术方案（Day 5）：技术路线 + 数据流图 + 实现期硬约束（T1–T8） |
| `.gitignore` | 忽略规则，确保 `.env` 等本地文件不会被上传 |
| `.env` | 本地环境变量（**已被忽略，不在仓库里**），Day 23 接数据库时启用 |

## 怎么跑起来

### 方式 A · 直接双击（最省事）

双击 `index.html` 就能用。**不需要任何服务器、依赖或构建步骤。**
这是 `TECH_DESIGN.md` 定下的技术路线，用来满足 PRD 的 AC-1（双击就能打开）。

### 方式 B · 起一个本地服务器（地址栏是 localhost）

在**项目目录**下打开 PowerShell，执行：

```powershell
python -m http.server 8000
```

然后浏览器打开：**http://localhost:8000**

- 想结束它：回到那个窗口按 `Ctrl + C`
- 页面本身不发任何外部网络请求（AC-10），所以方式 A 和方式 B 效果一样

## 数据存在哪

存在浏览器自带的本地存储里，键名是 `habit-board/v1`（带版本号，以后字段变了能区分新旧数据）。

- 关掉浏览器再打开，数据还在（AC-9）
- 换一个浏览器、或把文件挪到别的文件夹，数据不跟着走——这是本期接受的限制，见 `TECH_DESIGN.md` 第 7 节

## 快速自测（30 秒）

1. 首次打开：应看到「还没有习惯，先加一个试试」加一个示例按钮（AC-2）
2. 点那个示例按钮 → 习惯出现在列表里（AC-8）
3. 点习惯右边的圆圈 → 这一行变成已完成，顶部计数和「本周强度」立刻跟着变，不用刷新（AC-4）
4. 在底部输入框打一句话按回车 → 立刻出现在待办最上面，输入框被清空（AC-7）
5. 点习惯名字 → 打开详情，点一个**过去**的日期格子能补勾、再点一下取消；点**未来**的格子没有反应（AC-11）

## 提交规范

提交标题以「Day N｜」开头，例如：

```
Day 2｜初始化仓库并添加 index.html 占位页
```

正文两行：改了什么 / 加了什么。
