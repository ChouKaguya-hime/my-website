# AGENTS.md — 项目规则（工作手册）

> 这是给 AI 助手看的"工作手册"。第一天就把规矩写进来，以后每次对话 AI 都按这套来，不会跑偏、不会自作主张乱改。

## 项目是什么
- 名称：vibe-coding-week1（vibe coding 入门练习，第 1 周 Day 1）
- 目标：用自然语言指挥 AI 把想法变成能跑起来的软件，先练"立规则 + 搭环境"，不急于写业务代码。

## 沟通规则
- 默认用中文交流。
- 动手改任何文件之前，先用一句话说清楚"要改什么、为什么"。
- 改完立刻告诉我结果，不藏着。
- 我听不懂的术语，用大白话再讲一遍。

## 工作规则（为什么第一天就立规则）
- 小步快跑：一次只做一小件事，做完就验证，不一次搞太大。
- 规则写下来：关键决定、长期约定都存进本文件，保持前后一致。
- 卡住就跳过：某一项装不上/做不了，先记为待办，不卡在一项上。
- 不擅自动大改：涉及删除、覆盖、批量操作前必须先确认。

## 今天（Day 1）不做什么
- ❌ 不写业务代码
- ❌ 不建 Git 仓库（Day 2 才做）

## 环境验收清单（Day 1）
- [x] WorkBuddy —— 本对话即 WorkBuddy，运行中
- [x] Git —— 已安装（git version 2.55.0.windows.3）
- [x] Node.js —— 已安装（v22.22.2）
- [x] GitHub 账号 —— 已注册（用户名：ChouKaguya-hime）

## 待办
- [x] 注册 GitHub 账号 —— 完成（用户名：ChouKaguya-hime，2026-09-16）
- [ ] Day 2：建 Git 仓库，把本项目初始化并首次提交。

## 核心概念备忘（给自己看的）
- **vibe coding（氛围编程）**：你用日常说话的方式告诉 AI 想做什么，AI 负责把代码写出来，你负责看效果、提意见、定方向。不需要先学编程语言。
- **为什么第一天就立规则**：AI 没有长期记忆，每次对话都从零开始。AGENTS.md 就是给 AI 的固定"工作手册"，提前写规矩，后面每次都按规矩来。
- **skill（技能包）**：给 AI 装的"专业能力插件"，比如写 PPT、处理表格、生成图片、部署网站。基础 AI 只会聊天，装了 skill 才会干活。

## 工作区位置
- 路径：C:\Users\LM\Desktop\vibe-coding-week1

---

# 边界规则（Day 6 追加）

> 以下全部是 **Day 6 追加**的内容。上面 Day 1 写的规则**一字未改**，只是把原本没写明的边界补清楚。

## 追加 0｜规则文件现在有两份，以仓库里那份为准
- Day 1 的原件：`C:\Users\LM\Desktop\vibe-coding-week1\AGENTS.md`（保留，不动）
- **仓库里这份：`my-website/AGENTS.md`（Day 6 起以此为准）**
- 为什么搬进仓库：规则要跟着代码一起版本管理；Day 7 写代码时，AI 打开仓库就能读到规矩，不会跑偏。

## 追加 1｜当天只做当天任务，没听到「进入下一板块」就不越界
- **规则**：每次对话只推进「今天」这一个板块。**没听到斌说「进入下一板块 / 下一步 / 开始 Day N+1」之前，不许越界去做后面的事。**
- **防的是**：AI 自作主张扩大范围 —— 清单明明写着「今日不写代码」，它顺手把代码写了；或者今天的事刚做完，它自己接着把明天的也做了，导致产出清单和截图对不上。
- **怎么核验**：收尾时报一份「改动文件清单 → 各属于哪天」。只要有越界的文件，会立刻暴露在这份清单里。

## 追加 2｜写进文件的「事实」必须能指出核验动作，指不出就写「待确认」
- **规则**：任何被写进文件、记录或结论里的「事实」，都要能对应一个可复现的核验动作（一条命令 / 一次截图 / 一处引用）。指不出来的，一律改写成「待确认」，**不许当结论用**。
- **防的是**：把没验证的假设当成事实。**这个项目真实吃过一次**：Day 3 的记录里写了「Day 2 凭据应已在凭据管理器里」，结果不成立，白排查一轮；同类的还有 Day 3 装 VS Code 时 winget 退出码是 1，只看退出码就会误判成安装失败。
- **怎么核验**：随机抽查一条结论，问「这条是怎么确认的」。答不上来，就是违规。

## 检查项（余力加练｜把追加 2 改成队友能直接照着打钩的写法）
- [ ] 每条结论后面，能指出「怎么确认的」（命令 / 截图 / 引用）
- [ ] 指不出来的结论，都已经标成「待确认」，没有混在结论里
- [ ] 没有把「退出码 0」「进程起来了」当成「事情做成了」——要核验落地结果
