# TECH_DESIGN.md｜技术方案

> **项目**：`my-website`（GitHub：ChouKaguya-hime/my-website）
> **版本**：v1.0 ｜ **日期**：2026-09-20（Day 5）｜ **作者**：斌（与 AI 协作）
> **上游文档**：`PRD.md`（Day 4 做什么）、`research.md`（Day 3 需求研究）
> **本文只回答两个问题**：用什么技术实现 PRD 里的功能；数据从哪来、到哪去。
> 本文**不写代码**（Day 7 才写），也**不回改 PRD**。与 `PRD.md` 冲突时，产品问题以 PRD 为准。

---

## 0. 速览：一屏看完全貌

> 这一节是摘要，详细理由在第 2 节、第 3 节。

### 0.1 技术路线（一句话）

> **用原生 HTML + CSS + JavaScript 写一个单页应用，数据存在浏览器自带的本地存储里 —— 零框架、零依赖、零构建、零后端，双击 `index.html` 就能用。**

### 0.2 数据从哪来、到哪去

```text
┌──────────────────┐
│    用户（我）    │
└───────┬──────────┘
        │ (1) 操作：打字 / 点勾选 / 按回车
        v
┌───────┬──────────┐   (4) 我看到页面已经变成今天的了
│  浏览器里的网页  │<─────────────────────────────┘
│ HTML + CSS + JS  │                              │
└───────┬──────────┘                              │
        │ (2) 存进去（每次改动立刻存）            │
        v                                         │
┌───────┬──────────┐                              │
│  浏览器本地存储  │                              │
│  habits / todos  │                              │
└───────┬──────────┘                              │
        │ (3) 下次打开页面时读出来                │
        └─────────────────────────────────────────┘
```

**一句话读懂这张图**：数据从**我的手指**来，进了**网页**，落到**浏览器本地存储**里住下；下次打开页面时，数据从存储里被**读回网页**，画成页面，回到**我的眼睛**。

---

## 1. 前端 / 后端 / 数据库，各管什么

### 1.1 三层的通俗说法

用餐厅打比方：你坐下吃饭，全程只跟**服务员**打交道，但真正做菜的是**后厨**，食材存在**冷库**里。

| 层 | 通俗说法 | 餐厅里的对应 |
| --- | --- | --- |
| **前端** | 你看得见、点得到的部分，跑在你的浏览器里 | 服务员 + 菜单 + 餐桌 |
| **后端** | 你看不见的处理逻辑，跑在别人的服务器上 | 后厨 |
| **数据库** | 长期存数据的地方，通常在后端旁边 | 冷库 |

### 1.2 本期的分工结论

| 层 | 本期用不用 | 为什么 |
| --- | --- | --- |
| 前端 | ✅ **完全靠它** | PRD 第 6 节已经定死：只有一个用户、不需要联网。界面逻辑和数据都放在前端 |
| 后端 | ❌ 不用 | 没有多用户、没有需要保密的计算、没有跨设备同步。搭一套服务器换不来任何东西 |
| 数据库 | ❌ 不用 | 数据存在浏览器自带的**本地存储**里就够。真正的数据库是 **Day 23** 的任务 |

**「浏览器本地存储」是什么？**
浏览器给每个网页留了一小块属于它自己的存储空间，网页可以往里存文字，关掉浏览器也不会消失。
可以把它理解成**一块只属于这个网页的小仓库**——它替代了本期本来需要数据库的那部分职责。

⚠️ 但它不等于数据库：容量小（约 5MB）、不联网、换浏览器或换文件位置数据不通用。这些限制已记入 PRD 第 9 节风险表和本文第 7 节。

---

## 2. 技术路线是怎么定出来的

### 2.1 先看约束：PRD 里已经写死了四条

**技术选型不是「我觉得哪个酷就用哪个」，而是「哪个被允许用」。**

| # | 硬约束 | 出处 | 它排除了什么 |
| --- | --- | --- | --- |
| 1 | 直接双击 `index.html` 打开，3 秒内出现今日页 | PRD **AC-1** | 排除一切「必须先启动一个服务器」的方案 |
| 2 | 除页面自身文件外，**没有任何外部网络请求** | PRD **AC-10** | 排除一切从网上引 CDN 的写法 |
| 3 | 没有后端、没有账号，数据存浏览器本地 | PRD **第 6 节** | 排除一切服务端框架和数据库 |
| 4 | 只有一个用户，桌面 Chrome / Edge | PRD **第 2 节** | 不需要考虑老浏览器兼容，也不需要做移动端适配 |

### 2.2 结论：能同时满足这四条的路，只剩一条

> **原生 HTML + CSS + JavaScript + 浏览器本地存储。**

这条路线同时做到：不需要服务器（满足 1）、不需要联网（满足 2）、不需要后端和数据库（满足 3）、不需要兼容旧浏览器（满足 4）。

**这不是「最先进」的方案，是「唯一能全部满足」的方案。** 对一个只有自己用的工具，这就够了。

### 2.3 落选方案，以及落选原因

| 方案 | 为什么不选 | 违反了哪条 |
| --- | --- | --- |
| **React / Vue（走 CDN）** | 浏览器会去外部地址拉框架文件，Network 面板里会出现外部请求；中文网络环境下如果那个地址不通，页面直接白屏 | ❌ 违反 **AC-10** |
| **React / Vue（走打包工具）** | 打包产物用的是 ES Module，在 `file://` 协议下会被浏览器的跨域策略拦住 → 双击打开是白屏，必须先起一个本地服务器 | ❌ 违反 **AC-1** |
| **Next.js 等全栈框架** | 需要一个常驻的 Node 进程当后端。为一个用户搭这套，是拿大炮打蚊子 | ❌ 违反 **AC-10**、违背第 3 条约束 |
| **jQuery** | 它是为「摆平老浏览器差异」而生的。本期只需要管 Chrome / Edge，原生 JS 完全够用，引它等于白白多一个依赖 | 不违反，但没必要 |
| **小程序 / 手机 App** | PRD 第 3.2 节第 5 条已明确砍掉，第 2 周之后再说 | 已砍 |

### 2.4 一句话记住

> **约束先行，技术后选。** 先看需求允许什么，再挑技术；不是先挑技术，再想办法让它能跑。

---

## 3. 数据流图，逐条说明

### 3.1 四个环节

| 编号 | 谁到谁 | 发生了什么 | 对应 PRD |
| --- | --- | --- | --- |
| **(1)** | 我 → 网页 | 我打字、点勾选、按回车。这些动作变成数据进入网页 | F3 快速加待办、F1 勾选习惯、AC-7 |
| **(2)** | 网页 → 本地存储 | 网页**每次改动都立刻**把最新数据存回本地存储，不留「还没存」的状态 | G4 数据留存、AC-9 |
| **(3)** | 本地存储 → 网页 | 下次打开页面时，先把数据读出来，再算「今天该做哪些习惯」 | PRD 6.3 判定规则 |
| **(4)** | 网页 → 我 | 页面重画并显示出来。我看到的永远是刚存下的那份数据 | AC-4 即时反馈 |

### 3.2 两个真实场景走一遍

**场景 A：中午做完一件事，点一下勾**

```text
我点「喝水」这行的完成按钮
  → 网页把今天的日期加进这个习惯的完成记录里（在内存里改）
  → 网页立刻把整个习惯列表存回本地存储            ← (2)
  → 网页重画这一行：变成已完成样式
  → 顶部「今天已完成 3/7」和这行的「本周强度」马上跟着变   ← (4)
```

**场景 B：第二天早上打开电脑**

```text
我把 index.html 拖进浏览器打开
  → 网页先读本地存储，把昨天的习惯和待办全都取出来    ← (3)
  → 网页用「今天」这个日期，算出哪些习惯今天该出现（PRD 6.3：全都出现）
  → 网页用最近 7 天算每个习惯的「本周强度」（PRD 6.2）
  → 画成今天的页面，我看到                          ← (4)
```

**关键点：两条路都绕不开本地存储。页面永远不直接相信内存，也永远不直接相信存储——以「操作后立刻存、打开时重新读」为准。** 这样即使中途关掉浏览器，数据也不会丢在内存里。

### 3.3 数据不出本机

整张图里**没有任何一个环节连向外部网络**。数据从手指进，从屏幕出，中间只在你自己这台电脑的浏览器里打转。这就是 **AC-10** 的落地方式。

---

## 4. 数据长什么样

字段沿用 `PRD.md` 第 6.1 节，**不新增、不删减**。技术层只加一件事：这两个列表以什么形式存在本地存储里。

| 存储项 | 内容 | 说明 |
| --- | --- | --- |
| 习惯列表 | `habit[]` | 每个习惯：`id` / `name` / `freqType` / `freqCount` / `createdAt` / `doneDates` |
| 待办列表 | `todo[]` | 每条待办：`id` / `text` / `date` / `done` |

**两条技术约定**（Day 7 实现时照做）：

1. 两个列表整体转成一串文字存进去，用的时候再转回来——本地存储只能存文字，这是它的规矩。
2. 存储的**命名带版本号**（例如 `habit-board/v1`）。以后字段若有变化，靠版本号区分新旧数据，不至于把老数据读崩。

---

## 5. 文件怎么组织（Day 7 的计划，今天不建）

```text
my-website/
├── index.html          ← 唯一入口：今日页（含习惯详情弹层）
├── style.css           ← 全部样式
├── app.js              ← 全部逻辑：读写数据、算强度、重画页面
├── PRD.md              ← Day 4：做什么 / 不做什么 / 验收标准
├── TECH_DESIGN.md      ← 本文（Day 5）
├── research.md         ← Day 3：需求研究
└── README.md           ← 文件清单与提交规范
```

**为什么只有三个文件？** 本期就两个页面加一个弹层。文件多了要来回跳，反而不容易看清「一个入口看全」这件事有没有做到。

---

## 6. 这条路线带来的硬约束（写代码时必须守）

技术路线选了，就要接受它的规矩。以下每一条都是**技术路线的直接后果**，Day 7 写代码时逐条对照：

| # | 约束 | 为什么 | 违反了会怎样 |
| --- | --- | --- | --- |
| T1 | **不能引任何外部资源**——不能从网上加载字体、图标库、框架 | 保证 AC-10 零外部请求 | Network 面板出现外部请求，AC-10 直接不及格 |
| T2 | **不能用 ES Module 的写法**（`type="module"` 和 `import`） | `file://` 协议下浏览器会因跨域策略拦住模块加载 | 双击打开白屏 |
| T3 | **不能依赖构建工具**（不需要也没有打包步骤） | 保证 AC-1 双击就能开 | 多出一堆「先编译再打开」的步骤，AC-1 不及格 |
| T4 | **所有文件引用用相对路径**（写 `style.css`，不写 `/style.css`） | `file://` 下以 `/` 开头会被当成磁盘根目录 | 样式和脚本全部加载失败 |
| T5 | **图标只能用文字或 CSS 画**，不能引图标库 | 同 T1 | 图标不显示，或产生外部请求 |
| T6 | **字体只用系统自带字体**（字体栈里列几个系统字体名） | 同 T1 | 要么字体不生效，要么产生外部请求 |
| T7 | **每次数据改动后立刻写回存储，然后重画页面** | 保证 AC-4 即时反馈、AC-9 数据留存 | 刷新后改动丢失 |
| T8 | **不用 `fetch` 读本地文件** | `file://` 下读本地文件会被浏览器拦住 | 报错，功能不可用 |

---

## 7. 风险与应对

| 风险 | 如果真发生了会怎样 | 本期怎么应对 |
| --- | --- | --- |
| 本地存储的容量小（约 5MB），且清缓存会连带清掉 | 数据可能丢 | 本期数据量极小（几十条，远不到 1MB），够用；Day 23 换数据库 |
| 本地存储与「浏览器 + 文件路径」绑定 | 换个浏览器打开，或把文件挪到别的文件夹，数据不跟着走 | 本期就一台电脑一个浏览器，能接受；换数据库后自然解决 |
| `file://` 协议下部分浏览器 API 受限 | 某些写法会报错 | 已在第 6 节 T2 / T8 提前排除：不用模块加载、不用 `fetch` |
| 手算日期容易出 bug（不能用第三方日期库） | 强度算错、格子错位 | 只用到「今天」和「最近 7 天 / 28 天」两种计算，逻辑简单；Day 7 单独立一个函数专门管 |
| 「零构建」意味着没有报错提示和自动检查 | 写错了要自己看控制台 | 接受。本期代码量小，浏览器控制台够用 |

---

## 8. 附：方案变化时，哪些文件会受影响

这张表是「以防万一」的预案。**每一条都对应一个具体的改动场景**，不是要现在做：

| 如果哪天要改成…… | 直接受影响的文件 | 改动量 | 备注 |
| --- | --- | --- | --- |
| 加一个「本周视图」页面 | `index.html`、`app.js`、`style.css` | 小，只加不重写 | 仍是纯前端，路线不变 |
| 把数据改成多人共用 / 云端同步 | **全部重写** | 大 | 必须加后端 + 数据库，本文整份要重做 |
| 改成手机 App / 小程序 | **全部重写** | 大 | 换技术栈，`app.js` 的逻辑可以搬，界面要重做 |
| 接真正的数据库（Day 23） | `app.js` 里的「存储层」 | 中 | 只把「读写本地存储」那两个函数换掉，界面逻辑不动 |
| 加数据导出 CSV | `app.js` | 小 | 纯前端就能做，不需要后端 |
| 改成要起本地服务器才能跑 | `PRD.md` 的 AC-1、`README.md`、`app.js` 的 T2/T8 写法 | 中 | ⚠️ 先改 PRD 再改代码，别反过来 |
| 换用 React / Vue | **全部重写** | 大 | ⚠️ 需先改 PRD 的 AC-1（放弃双击打开）和 AC-10（允许引外部文件） |
| 加中文字体或图标库 | `index.html` | 小 | ⚠️ 会违反 T1 / AC-10，必须想清楚代价 |
| 部署到 GitHub Pages | 无（不用改代码） | 无 | 全是静态文件，直接能跑；但数据仍然只在各自浏览器里，不共享 |

**这张表的用法**：想改任何东西之前，先在左列找到最接近的一行，看它影响哪些文件、以及会不会撞上 PRD 的验收标准。

---

## 9. 变更记录

| 版本 | 日期 | 改了什么 |
| --- | --- | --- |
| v1.0 | 2026-09-20 | 首版（Day 5 产出）：定下「原生 HTML/CSS/JS + 浏览器本地存储」技术路线及其四条约束依据；画出数据流图；列出 8 条实现期硬约束、5 条风险、9 行方案变化影响表 |

---

**下一步**：Day 6 照计划推进；**Day 7 按 `PRD.md` 做出能双击打开的 MVP**，写代码时逐条对照本文第 6 节的 T1–T8。
